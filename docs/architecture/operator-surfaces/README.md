# Operator Surfaces

This folder contains docs for operator-facing surfaces such as:

- MCP
- CLI
- editor surfaces
- local workflows

These docs should describe what the surface exposes, what it does not expose, and which bounded system owns the truth behind it.
